[tool.black]
line-length = 100

[tool.isort]
profile = "black"
line_length = 100

[tool.coverage.run]
branch = true
source = ["src/code_explainer"]

[tool.coverage.report]
show_missing = true
skip_covered = true
