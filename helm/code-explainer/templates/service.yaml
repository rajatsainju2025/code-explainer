apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-explainer.fullname" . }}
  labels:
    {{- include "code-explainer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-explainer.selectorLabels" . | nindent 4 }}
